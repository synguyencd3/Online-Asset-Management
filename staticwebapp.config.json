{
    "navigationFallback": {
        "rewrite": "/index.html",
        "exclude": [
            "*.{jpg,gif,png}",
            "assets/*"
        ]
    },
    "routes": [
        {
            "route": "/*",
            "rewrite": "/",
            "statusCode": 401
        }
    ]
}